name: send testing data to api

on: [push]

jobs:

  hello-world:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: ./
        with:
          user_id: '1234'
          tha_no: '20'
